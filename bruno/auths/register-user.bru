meta {
  name: register-user
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/api/users/register
  body: json
  auth: inherit
}

body:json {
  {
    "fullname":"ethenkem",
    "email":"{{process.env.EMAIL_ADDRESS}}",
    "password":"123456789"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

meta {
  name: release-payment
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/payment/release/:paymentId
  body: none
  auth: bearer
}

params:path {
  paymentId: 
}

auth:bearer {
  token: {{token}}
}
